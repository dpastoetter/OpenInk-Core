{
  "name": "browseros",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "preact": "^10.19.2"
  },
  "devDependencies": {
    "@preact/preset-vite": "^2.8.0",
    "jsdom": "^24.0.0",
    "typescript": "~5.3.0",
    "vite": "^5.0.0",
    "vitest": "^1.2.0"
  }
}
